
/**
 * Represents the Expo configuration object.
 */
export const expo = {
  /**
   * The `mode` property specifies the mode of the Expo app.
   * The possible values are `development`, `production`, and `demo`.
   */
  mode: 'demo'
};
